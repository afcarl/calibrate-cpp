Code for calibrating cameras. The code acquires checkerboard images from either the 
file system (a directory path) or from a webcam. Only .jpg and .jpeg images are used
in case a file system is specified.

Compiling
===============

$ make


Usage
=========

$ bin/calibrate -h


